auto-install-peers=true
shamefully-hoist=false

# Approve build scripts for native binaries (Prisma engines, esbuild)
# This prevents the interactive "approve-builds" prompt on every install
approve-builds-automatically=true
